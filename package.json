{
  "name": "starbugs-automacao-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:dependence": "npm install",
    "check:updates:up": "npx npm-check-updates -u",
    "check:updates": "npx npm-check-updates",
    "cy:open": "cypress open",
    "cy:run": "npx cypress run",
    "cy:run:allure": "npx cypress run --config video=false --env allure=true",
    "allure:generate": "allure generate allure-results --clean",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "allure:report:open": "allure generate allure-results --clean -o allure-report && allure open",
    "allure:clear": "if exist allure-results rmdir /q /s allure-results && if exist allure-report rmdir /q /s allure-report && if exist cypress\\screenshots rmdir /q /s cypress\\screenshots && if exist cypress\\videos rmdir /q /s cypress\\videos",
    "run:test:allure-report:generate": "npm run cy:run:allure && npm run allure:report",
    "run:test:allure-report:generate:open": "npm run cy:run:allure && npm run allure:report:open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.14.0",
    "@shelex/cypress-allure-plugin": "^2.40.1",
    "allure-commandline": "^2.25.0"
  }
}
